server:
  port: 8085

spring:
  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true